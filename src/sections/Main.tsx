import React from "react";
import Button from "../components/Button";

const Main: React.FC = () => {
  return (
    <div className="container">
      <Button create={true} />
    </div>
  );
};

export default Main;
